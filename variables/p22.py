#INDENTATION IN PYTHON 
"""In Python, indentation is used to declare a
block. If two statements are at the same indentation level, then they are the part of
the same block """

a=1
val1=int(input('write 1 for jay ram'))

if a!=val1:
 b='jay shayam'
 print(b,' because you write :',val1)
else:
 b='jay ram'
 print(b,' because u write : ',val1)
